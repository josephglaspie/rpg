apiVersion: v1
kind: Service
metadata:
  name: rpg-game-service
  namespace: rpg-game
  labels:
    app: rpg-game
spec:
  selector:
    app: rpg-game
  ports:
  - name: http
    port: 80
    targetPort: 8081
    protocol: TCP
  type: ClusterIP